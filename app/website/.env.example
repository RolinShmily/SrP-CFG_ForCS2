# AI 配置助理（/commands 右侧面板）人机验证 —— 构建期注入前端
# 复制为 .env 后在本地 dev/build 使用；Site Key 是公开标识，可提交到 CI。
# 获取方式：Cloudflare Dashboard → Turnstile → Add site（域名填 srprolin.top / cfg.srprolin.top）
PUBLIC_TURNSTILE_SITE_KEY=0x4AAAA...

# 部署参考（GitHub Actions secrets，勿提交真实值；见 .github/workflows/deploy-website.yml）：
#   PUBLIC_TURNSTILE_SITE_KEY   = Turnstile Site Key（公开，前端校验用）
#   CLOUDFLARE_TURNSTILE_SECRET = Turnstile Secret Key（Worker 端 siteverify 用，仅服务端）
#   CLOUDFLARE_API_TOKEN        = Cloudflare API Token（需 Workers Scripts:Edit 权限）
#   CLOUDFLARE_ACCOUNT_ID       = Cloudflare 账号 ID
