{
  "identifier": "default",
  "description": "主窗口默认能力：核心 API + opener（打开外部链接/文件路径） + dialog（文件选择）",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
