<div align="center">

[English](SECURITY.md) | [简体中文](SECURITY.zh-CN.md)

</div>

# 安全政策

## 支持范围

仅最新的 `v3.x` 发布线接受安全修复。更早的 tag、第三方 fork，以及用户自行重新打包的
ZIP 均不在支持范围内。

| 版本 | 是否支持 |
| :--- | :---: |
| 最新 `v3.x` 正式版 | ✅ |
| 更早版本 | ❌ |

## 漏洞报告

**请勿为安全问题提交公开 Issue。** 请改用以下任一渠道：

1. **GitHub 私密报告** —— [Report a vulnerability](https://github.com/RolinShmily/SrP-CFG_ForCS2/security/advisories/new)
   （推荐：报告、讨论与修复都在同一处闭环）。
2. **邮箱** —— `rol1n@srprolin.top`。

请尽量提供：受影响版本、所属组件（桌面套件 / 官网 / 配置包）、复现步骤，
以及可用的 PoC 或日志输出。

我们会在 3 个工作日内确认收到。随后会与你核实问题、商定修复与披露时间线，
并在发布说明中署名致谢（除非你希望匿名）。

## 范围之内

桌面套件会读取本机并向你的 CS2 安装目录写入文件，因此以下问题均按安全漏洞处理：

- **路径穿越 / 任意文件写入** —— 在 ZIP 解压、组件安装、快照恢复或更新应用过程中，
  写入到探测到的 CS2 目录之外，或覆写 `user/custom.cfg`。
- **任意代码执行**、提权，或通过安装器、更新器或构造的输入包实施 DLL / 二进制劫持。
- **发布完整性** —— 发布产物被篡改、签名或哈希校验被绕过、更新通道可被重定向至
  不受信任的来源。
- **本地数据的不当处理** —— 读取或外传任何超出应用职责范围（Steam / CS2 路径）的数据。
- **官网与 AI 助手** —— Cloudflare Workers 接口（提示注入导致密钥泄露、未授权访问
  KV / Vectorize、SSRF、XSS）。
- **仓库内的机密泄露** —— 任何被提交的 Token、密钥或凭据。

## 范围之外

- Counter-Strike 2 或 Steam 引擎本身的行为，包括 `VCFG` 格式 —— 请向
  [Valve](https://www.valvesoftware.com/en/security) 报告。
- 需要先决条件（本机已被攻陷）或物理接触才能利用的问题。
- 写入配置这一预期行为：应用*本来就*会修改你自己 CS2 目录下的文件，
  正因如此它才会在部署前展示文件差异预览。
- 第三方制作的配置包。本政策仅覆盖由本项目发布工作流产出的包。
- 无实际影响的安全加固缺失（例如缺少 CSP 响应头、日志过于详细、自 XSS）。

## 关于你的数据

桌面套件完全本地运行。Steam、CS2 与账号路径仅在本机探测，不会上传到任何地方。
联网行为仅限：获取发布元数据、下载官方配置包，以及（官网上的）AI 助手。

## 披露原则

我们遵循协同披露：请给予我们合理的时间窗口在公开细节前完成修复。
修复会随下一个 `v3.x` 版本发布，并在发布说明中明确标注。

感谢你帮助守护 SrP-CFG 用户的安全。
