# v1.1.3
!!!解决重大BUG!!!：
- 跑图模式下投掷物无轨迹，默认开启X光即可解决。
优化各项功能初始配置：
- 默认屏蔽下方小字按键提示，可以输入`show`开启，`noshow`关闭
- 默认屏蔽投掷物轨迹预测小窗，防止demo录制时的奇异；输入`grenadeoff`关闭，`grenadeon`开启
- 默认关闭投掷物落点预测小窗，可通过快捷键`⬅`开关
- 默认开启X光，添加`xray`别名开关投掷物轨迹
- 默认关闭弹着痕显示，通过快捷键`⬆`开关
- 添加BOT默认静止