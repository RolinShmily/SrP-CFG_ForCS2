# ============================================
# SrP-CFG 发布包配置
# ============================================

packages:
  # 唯一发行包：Runtime、用户窗口、内置 Preset 与重置基线。
  runtime_core:
    display_name: "Runtime Core"
    zip_name: "SrP-CFG_Runtime_Core"
    description: "完整功能 Runtime、用户 custom.cfg、内置 Preset 案例与 Valve 重置基线"
    files:
      - "default/autoexec.cfg"
      - "default/video/"
      - "default/annotations/"
      - "default/srp-cfg/"
