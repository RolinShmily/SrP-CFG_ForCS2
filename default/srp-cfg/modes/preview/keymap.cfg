// Optional preview workspace keys. Use srp_preview for settings without these bindings.
bind "z" "toggle cl_drawhud;echo HUD状态已更新!"
bind "x" "toggle cl_lock_camera;echo 摄像机视角固定状态已更新!"
bind "c" "toggle r_dof_override;echo 景深状态已更新!"
bind "v" "changeblur"
bind "downarrow" "changefov"
bind "-" "changeang--"
bind "=" "changeang++"
