// Guide-maker mode settings. Physical keys are isolated in keymap.cfg.
sv_cheats 1                                  // Required for local authoring tools.
sv_allow_annotations_access_level 2         // Allow viewing and editing annotations.
annotation_auto_load 1                      // Auto-load local annotation guides.
echo [SrP-CFG] Guide-maker settings applied; use srp_guidemake_keys for the workspace keymap.
