echo ═════════════════ built-in preset examples ═════════════════
echo srp_apply_default  : 作者推荐设置与按键
echo srp_apply_echo     : Default + Echo 示例差异
echo srp_apply_yszh     : Default + YSZH 示例差异
echo srp_apply_visionl  : Default + VisionL 示例差异
echo 在 user/custom.cfg 顶部启用其中一个命令，再把个人差异写在它下面。
echo 这些命令不会再次执行 custom.cfg，因此可安全作为用户配置的起点。
echo 只发行 Runtime Core；内置案例不再对应独立下载包或自动启动层。
echo ════════════════════════════════════════════════════════════
