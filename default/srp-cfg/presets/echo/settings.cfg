// Echo example: personal preference differences layered on Default.
sensitivity 1.10                                  // Echo mouse preference.
cl_teamcounter_playercount_instead_of_avatars 0  // Keep avatars instead of player count.
safezonex 1                                      // Use the full horizontal HUD area.
safezoney 1                                      // Use the full vertical HUD area.
cl_hud_color 5                                   // Echo HUD color.
cl_showloadout 1                                 // Always show the equipment bar.
cl_color 4                                       // Preferred teammate color.
