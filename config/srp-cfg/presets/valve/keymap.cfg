// Ask the current CS2 build to reload its own shipped user_keys_default.vcfg.
// This resets normal and analog bindings without distributing or editing a user VCFG file.
binddefaults // 恢复当前 CS2 版本自带的默认按键绑定。
