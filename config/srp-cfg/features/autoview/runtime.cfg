// Persistent autoview command vocabulary.
// Runtime loads this file every launch so synced bindings remain executable.
alias "view_0" "v06" // 将 alias view_0 转发到 v06。
alias "view_1" "v00" // 将 alias view_1 转发到 v00。
