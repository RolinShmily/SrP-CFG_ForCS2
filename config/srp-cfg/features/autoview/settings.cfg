// Reset autoview's internal view mapping without changing physical keys.
exec srp-cfg/features/autoview/runtime.cfg // 加载武器自适应视角的 Runtime 命令。
echo [SrP-CFG] autoview commands ready; use srp_autoview_keys to install its keymap. // 在控制台输出应用完成提示。
