// Reset Zeus aliases without changing physical keys.
exec srp-cfg/features/zeus/runtime.cfg // 加载电击枪自动切换的 Runtime 命令。
echo "[SrP-CFG] Zeus commands ready; use srp_zeus_keys to install the weapon keymap." // 在控制台输出应用完成提示。
