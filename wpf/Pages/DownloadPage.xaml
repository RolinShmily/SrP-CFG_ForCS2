<Page x:Class="SrPInstaller.Pages.DownloadPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="800"
      Title="下载">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="24,16,24,20">

            <TextBlock Text="配置包下载" FontSize="22" FontWeight="Bold" Margin="0,0,0,4"/>
            <TextBlock Text="从 GitHub Releases 获取最新配置包，下载后拖入安装页即可安装" FontSize="13"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" Margin="0,0,0,20"/>

            <ItemsControl ItemsSource="{Binding DownloadItems}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ui:Card Padding="20" Margin="0,0,0,10">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Name}" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,2"/>
                                    <TextBlock Text="{Binding FileName}" FontSize="11" FontFamily="Consolas"
                                               Foreground="{DynamicResource TextFillColorTertiaryBrush}" Margin="0,0,0,4"/>
                                    <TextBlock Text="{Binding Description}" FontSize="12"
                                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" TextWrapping="Wrap"/>
                                    <TextBlock Text="{Binding Note}" FontSize="11" FontStyle="Italic"
                                               Foreground="{DynamicResource TextFillColorTertiaryBrush}" Margin="0,2,0,0">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Note}" Value="{x:Null}">
                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>
                                <ui:Button Grid.Column="1" Content="&#xE896; 下载"
                                           FontSize="12" Padding="16,6" VerticalAlignment="Center" Appearance="Primary"
                                           Command="{Binding DataContext.OpenUrlCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                           CommandParameter="{Binding DownloadUrl}"/>
                            </Grid>
                        </ui:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Tip -->
            <ui:Card Padding="16" Margin="0,12,0,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xE7BA;" FontFamily="Segoe MDL2 Assets" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBlock Text="下载 ZIP 后，切换到「安装」页面将文件拖入即可完成配置部署" FontSize="12"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
                </StackPanel>
            </ui:Card>
        </StackPanel>
    </ScrollViewer>
</Page>
